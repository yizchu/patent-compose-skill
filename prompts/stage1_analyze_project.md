# Stage 1: 分析项目，初步生成“权利要求树”

## 任务目标
遍历项目的所有文件，判断文件类型，根据图片和文字（如果是软件项目，还需要关注代码）分析项目架构，提取技术细节，最终生成一棵完整的“权利要求树”。

## 工作流程

### Step 1：收集文件信息
执行：
```bash
python "${SKILL_DIR}/scripts/analyze_project.py" get_files_info <项目根目录> <项目根目录>
```
输出目录下会生成 `project files` 目录，该目录**完全镜像了项目的目录结构**，并额外包含一个 `files_info.json` 文件。
- `files_info.json`：以树形结构记录整个项目的文件信息，根节点对应项目根目录，叶节点包含文件的基本信息（文件名、扩展名、前几行内容等），子节点为父节点对应目录下的子目录或文件。
- **其余文件夹**：与项目目录结构一一对应。例如，若项目中某脚本文件位于 `{项目根目录}/backend/routers/xxx.py`，则输出目录下也会有 `project files/backend/routers/` 路径。
- **最底层的文件夹**：以原项目文件命名（如 `xxx.py` 对应 `xxx.py/` 文件夹），其中包含从该文件中提取出的所有文本和图片：
  - 文本内容统一保存在 `content.md` 中
  - 图片直接保存在该文件夹内，依次按自增序号命名`image1.png`、`image2.png`……。

### Step 2：项目第一次分析
1. **判断文件类型**
**`Read`** `${输出目录}/project files/files_info.json`，判断其中的文件：
- 哪些是已有交底书/专利申报建议/创新点汇总
- 哪些是项目文档（架构说明、设计文档、流程图等）
- 哪些是配置文件
- 哪些可以忽略
2. **精读文档文件**
遍历 `${输出目录}/project files` 目录下的所有文件夹，按优先级选取重要文件进行精读，提炼项目的技术细节（如：模块组成、函数调用关系、数据流向、机械结构……）、识别潜在的专利点（如：独特算法、创新结构……），同时标注每个专利点的来源文件（使用其在项目目录下的相对路径）
3. **保存分析结果**
将分析结果保存到 `${输出目录}/materials/disclosure-v1.md` 文件中。如果没有分析出任何结果，也要创建一个空文件。

### Step 3：项目图片分析（如果有图片文件的话）
1. **精读图片文件**
遍历 `${输出目录}` 目录下的所有文件夹（包括`patent-compose output`目录），**`Read`** 所有的图片文件、提取图片内容，尝试从这些图片中提炼项目的技术细节（如：模块组成、函数调用关系、数据流向、机械结构……）、识别潜在的专利点（如：独特算法、创新结构……），同时标注每个专利点的来源文件（使用其在项目目录下的相对路径）。
2. **保存分析结果**
**`Read`** `${输出目录}/materials/disclosure-v1.md`，将本次图片分析结果与之整合，覆盖 `${输出目录}/materials/disclosure-v1.md` 文件。

### Step 4：项目代码分析（如果有代码文件的话）
1. **安全检测**
执行以下命令进行代码敏感信息扫描：
```bash
python "${SKILL_DIR}/scripts/analyze_project.py" examine_code_file <项目根目录>
```
等待命令执行完成，仔细阅读输出的检测报告。若检测到敏感信息（如 API 密钥、密码、Token、数据库连接串等），**必须**向用户明确列出。**暂停后续步骤**，等待用户回复确认再继续。

2. **对核心代码文件进行深度分析**
遍历 `${输出目录}` 目录下的所有文件夹（`patent-compose output`目录除外），分析所有的代码文件。代码文件范围仅限于 `${SKILL_DIR}/scripts/analyze_project.py` 中 `CODE_EXTENSIONS` 常量定义的后缀文件，**严禁读取**该常量之外的任何文件类型。

分析时需依项目领域灵活识别以下关注点（包括但不限于）：
- 核心算法与策略实现
- 业务逻辑与流程编排
- 数据处理与转换
- 规则引擎与决策逻辑
- 接口与集成设计
- 状态机与调度机制
- 性能优化与缓存策略
- 安全与权限控制

3. **保存分析结果**
**`Read`** `${输出目录}/materials/disclosure-v1.md`，将本次代码分析结果与之整合，覆盖 `${输出目录}/materials/disclosure-v1.md` 文件。

**提示**：
当代码文件数量庞大时，采用分批策略：
- **先读目录结构**：先通过 `files_info.json` 了解整体架构，识别核心模块/服务目录
- **优先处理核心**：先分析与项目核心功能最相关的文档和代码（如架构设计文档、主入口文件、核心业务逻辑）
- **逐批深入**：按模块分批精读，每完成一个模块就将分析结果追加到 `disclosure-v1.md`，避免一次性加载过多内容
- **聚焦专利点**：对于明显属于第三方库、依赖包、自动生成代码、常规工具类的内容，可以快速跳过，集中精力分析项目独有的技术实现


### Step 5：初步生成“权利要求树”
**`Read`** `${输出目录}/materials/disclosure-v1.md` 文件，该文件已包含项目的分析结果。基于分析结果，构建一棵完整的“权利要求树”，用JSON格式表示，保存至 `${输出目录}/materials/claim-tree-v1.json` 文件。

**核心原则**：
- **尽可能多地提取专利点**：哪怕是不确定是否具备新颖性的技术点，也要先纳入树中
- **层级清晰**：独立权利要求应包含完整的技术方案，从属权利要求逐层细化具体实现。例如，独权描述方法整体流程，从权1细化步骤A的具体实现，从权2细化步骤B的具体实现。
- **技术特征完整**：技术特征的所有技术细节应在从属权利要求中充分展开。
- **详细描述**：每个节点必须有几句话的详细描述，说明特征、作用、具体实现方式等
- **保护范围合理**：独权不要包含过多非必要技术特征，避免保护范围过窄。例如，独权写核心算法流程，而非具体参数配置。

以软件项目为例。先从分析结果中提取项目的所有主要模块/服务。对每个模块，拆解其包含的所有流程/步骤，对每个流程，提取所有技术实现细节。“权利要求树”示例如下：

```json
{
  "一种关系型数据库转图数据库的方法": {
    "源库元数据映射与模型生成": {
        "框架连接与模型迁移": "采用Django应用程序框架连接关系型数据库，迁移生成用于以模型形式展示表结构及表间关系的模型文件",
        "模型文件修正优化": {
            "索引标记补全": "为关系型数据库中具有单独索引、但模型文件未体现的表字段添加索引标记",
            "数据类型修正": "修正数据迁移后发生错误变化的字段数据类型，保证两端类型映射一致",
            "默认值补全": "为关系型数据库中具有默认值、但模型文件未体现的表字段补充默认值配置"
          }
    },
    "图数据库元数据构建": {
        "点标签生成": {
            "字段遍历与外键过滤": "遍历模型文件下的所有字段，判断并跳过外键字段，仅保留普通属性字段",
            "字段类型映射转换": "根据预设的映射关系，将当前字段类型转换为图数据库支持的类型格式",
            "建点语句合成": "将模型对应的表名与所有非外键字段结合，生成图数据库的创建点标签语句"
        },
        "边类型生成": {
            "外键字段遍历识别": "遍历模型文件下的所有字段，识别出全部外键字段",
            "边命名与创建语句生成": "以「表名+外键字段名」组成边类型名称，对应创建图数据库的边类型语句"
        },
        "索引体系构建": {
            "点标签索引建立": "以模型文件对应的表名，建立图数据库的点标签索引",
            "复合/联合索引转换": "检查模型中的复合索引与联合索引，对应建立点属性复合索引",
            "字段级索引遍历生成": "遍历所有字段：外键字段建立边索引，带索引的非外键字段建立点属性索引",
            "遍历完成校验": "判断所有字段遍历完成后结束索引建立，否则返回继续遍历"
        }
    },
    "全量数据分页迁移": {
        "主键外键识别与映射构建": {
            "主键类型判定": "遍历模型所有字段识别主键，检查主键是否在联合唯一索引中，区分为联合主键或单主键",
            "外键关联信息提取": "识别外键字段，获取外键关联的表名与对应字段类型",
            "字段关系映射字典": "遍历过程中将字段、外键关联表名、字段类型关系保存到字段关系映射字典中"
        },
        "游标分页查询": {
            "游标初始化": "使用主键字段与外键字段信息初始化游标，确定分页查询的查询语言",
            "批量实例集拉取": "执行分页查询语句，获取当前批次的模型实例集，判断是否存在未迁移数据",
            "游标动态更新": "每批次数据处理完成后，通过主键字段对应值更新游标，进入下一批次查询"
        },
        "点边数据批量插入": {
            "点唯一标识符生成": "根据主键字段值结合表名生成哈希值，作为当前点在图数据库中的唯一标识符",
            "边关系与指向确定": "根据外键字段值计算指向点标识符，确定边的起点、终点与对应边类型",
            "点属性数据转换": "依据字段关系映射字典，提取实例属性值并完成类型转换，生成点属性数据",
            "批量插入语句执行": "批量生成点和边的插入语句并执行，完成当前批次数据写入"
        }
    },
    "迁移数据一致性验证": {
        "元数据映射验证": "从关系型数据库提取元数据，建立与图数据库的映射关系，校验点与边的对应关系是否匹配",
        "数据量统计验证": "分别统计关系型数据库各表数据量与图数据库各点标签数据量，判断数据量是否完全一致",
        "查询结果比对验证": "执行相同业务逻辑的数据查询，比对关系型数据库与图数据库的查询结果是否一致"
    }
  }
}
```

### Step 6：用户确认
提醒用户检查 分析结果 和 “权利要求树” 是否符合预期，同时提示用户“若会话上下文接近上限，可以在新会话继续之后的步骤，但要记得告诉智能体已经完成了stage1”，确认无误后Stage1才算完成。如果用户有异议，需要结合用户反馈和已有分析结果进行修正，直到用户满意为止。

## 关键要求
- **真实性原则**：所有分析必须严格基于项目的真实文件，不得凭空捏造或臆测不存在的技术方案
- **全面覆盖**：不能遗漏任何技术细节，只有明显属于行业常规实现的内容才可以省略
- **术语标准**：必须采用所属技术领域的通用技术术语，优先使用国家标准、行业标准规定的规范名词，以及国际专利分类表（IPC）中的标准技术术语。国家有统一规定的自然科学名词，应当采用官方统一术语；无官方规定的，可采用本领域约定俗成的表述，不得自行编造非通用词汇。禁止使用口头俗称、网络热词、行业黑话、非技术类表述替代专业术语。
- **通俗易懂**：用清晰直白的语言描述技术方案，确保非本领域技术人员也能理解，避免空泛赞美、营销口号、万能句式、结构模板、正确废话等具有“AI味”套话。